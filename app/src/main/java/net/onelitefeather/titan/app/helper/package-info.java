/**
 * Application-side helpers that turn Titan's domain types into what a player actually sees — the
 * navigator menu above all.
 *
 * <p>The rules the navigator follows live in
 * {@link net.onelitefeather.titan.common.navigator}; this package only wires them to Aves and to
 * the running server.
 */
@NotNullByDefault
package net.onelitefeather.titan.app.helper;

import org.jetbrains.annotations.NotNullByDefault;
